# Supabase (optionnel - pour persistance en base de données)
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# WebSocket Server (optionnel - pour multijoueur réseau)
# En local: ws://localhost:8080 (défaut, auto-détecté)
# En production sur même domaine: laissez vide (auto-détecté avec wss://)
# Sinon: https://your-websocket-server.com
# VITE_WS_URL=wss://your-websocket-server.com
